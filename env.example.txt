# Copia este archivo como ".env" y completa tus valores reales.
# NUNCA subas el archivo .env real a un repositorio público.

# --- Cerebro del asistente: "ollama" (local, GRATIS) o "anthropic" (Claude, de pago) ---
LLM_BACKEND=ollama

# --- Ollama (solo si LLM_BACKEND=ollama) ---
# Modelo local a usar. Tiene que soportar "tool calling": llama3.1, qwen2.5,
# qwen3, mistral-nemo, entre otros. Bajalo antes con: ollama pull llama3.1
OLLAMA_MODEL=llama3.1
OLLAMA_HOST=http://localhost:11434

# --- Anthropic / Claude (solo si LLM_BACKEND=anthropic) ---
# Tu API key de Anthropic (https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
CLAUDE_MODEL=claude-sonnet-5

# Nombre con el que despertás al asistente al hablar (solo para el saludo, no es wake-word)
ASSISTANT_NAME=Jarvis

# Carpeta de proyectos donde OpenCode va a trabajar por defecto
OPENCODE_WORKDIR=C:\Users\TU_USUARIO\Projects

# Hotkey para activar el micrófono (push-to-talk). Ver docs de la lib "keyboard".
PUSH_TO_TALK_HOTKEY=ctrl+space

# --- Control remoto desde el celular ---
# Token secreto que tu celular tiene que mandar para poder controlar el PC.
# Invéntate uno largo y random, ej: usa https://www.uuidgenerator.net/
REMOTE_TOKEN=cambia-esto-por-algo-secreto-y-largo

# Puerto donde corre el servidor del control remoto
REMOTE_PORT=8765
